services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: lex
      POSTGRES_USER: lex_owner
      POSTGRES_PASSWORD: lex_owner_pass
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
